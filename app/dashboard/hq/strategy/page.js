'use client';

import StrategyBoard from '@/components/shared/Strategy/StrategyBoard';

export default function HQStrategyPage() {
    return (
        <div className="h-full bg-[#0E0E18] overflow-hidden -ml-6">
            <StrategyBoard />
        </div>
    );
}
